/**
 * تشخیص «حالت نمایشی» برای نمایش اعتبارنامهٔ آزمایشی.
 *
 * ══════════════════════════════════════════════════════════════
 *  چرا این ماژول حیاتی است
 * ══════════════════════════════════════════════════════════════
 * صفحهٔ ورود مدیر نام کاربری و رمز نمایشی را نشان می‌دهد تا در
 * فاز توسعه بتوان پنل را آزمود. اگر همان بلوک روی هاست عمومی
 * برود، اعتبارنامهٔ پنل مدیریت برای همه قابل خواندن است — و چون
 * گارد فعلی فقط سمت کلاینت است، یعنی پنل عملاً باز است.
 *
 * `process.env.NODE_ENV` در زمان بیلد جایگزین می‌شود، پس با
 * `next build` کل بلوک راهنما از باندل **حذف** می‌شود
 * (dead-code elimination) — نه اینکه فقط پنهان شود.
 *
 * ── چگونه بررسی کنید ──────────────────────────────────────────
 *   npm run build && grep -r "saite-demo" .next/static
 *   خروجی باید خالی باشد.
 */

/**
 * آیا اعتبارنامهٔ نمایشی نشان داده شود؟
 *
 * فقط در توسعهٔ محلی. عمداً از متغیر محیطی قابل تنظیم استفاده
 * نمی‌کنیم تا کسی نتواند اشتباهاً روی production روشنش کند.
 */
export const IS_DEMO_MODE = process.env.NODE_ENV === 'development'
