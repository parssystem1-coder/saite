import type { ShippingMethod } from '@/types/shipping'
export function validateShippingMethod(m:ShippingMethod):string[]{const e:string[]=[];if(!m.name.trim())e.push('نام روش ارسال الزامی است');if(!m.carrierId)e.push('شرکت حمل را انتخاب کنید');if(m.paymentMode==='prepaid'&&m.pricingModel==='flat_rate'&&(m.flatRate??0)<0)e.push('هزینه ثابت نامعتبر است');if(m.paymentMode==='cash_on_delivery'&&m.pricingModel==='free')e.push('پس‌کرایه و مدل رایگان را هم‌زمان فعال نکنید');if(m.estimatedMinDays<0||m.estimatedMaxDays<m.estimatedMinDays)e.push('بازه زمان تحویل نامعتبر است');if(m.paymentMode==='free'&&!m.freeOverOrderTotal&&m.pricingModel!=='free')e.push('شرط ارسال رایگان را مشخص کنید یا روش را عمومی رایگان کنید');return e}
