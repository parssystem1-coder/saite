import type { SeoChecks } from '../product-editor.types';
import { SeoScore } from './SeoScore';
import { editorSurfaceClass } from './EditorSection';

export function ProductSidebar({ score, checks, imageCount, attributeCount, schemaCount, status, onStatusChange }: { score: number; checks: SeoChecks; imageCount: number; attributeCount: number; schemaCount: number; status: string; onStatusChange: (status: string) => void }) {
  return <aside className="self-start lg:sticky lg:top-24"><div className={`${editorSurfaceClass} mb-4 p-4`}><div className="mb-3 flex items-center justify-between"><b className="text-sm">انتشار</b><span className="rounded-full bg-[hsl(var(--stock-low)/.14)] px-2 py-1 text-[10px] text-[hsl(var(--stock-low))]">{status}</span></div><select className="min-h-10 w-full rounded-md border border-[hsl(var(--border))] bg-[hsl(var(--input))] px-3 text-sm" value={status} onChange={e => onStatusChange(e.target.value)}><option>پیش‌نویس</option><option>منتشر شده</option><option>زمان‌بندی‌شده</option></select></div><SeoScore score={score} checks={checks} /><div className={`${editorSurfaceClass} mt-4 p-4 text-xs`}><div className="flex justify-between border-b border-[hsl(var(--border))] py-2"><span className="text-[hsl(var(--muted-foreground))]">ویژگی فنی</span><b>{attributeCount}</b></div><div className="flex justify-between border-b border-[hsl(var(--border))] py-2"><span className="text-[hsl(var(--muted-foreground))]">تصویر</span><b>{imageCount}</b></div><div className="flex justify-between py-2"><span className="text-[hsl(var(--muted-foreground))]">اسکیما</span><b>{schemaCount}</b></div></div></aside>;
}
