import {describe,expect,it} from 'vitest'
import {assertPaymentAmount,assertRefundAmount,canTransitionPayment,chooseProvider,validateProvider} from '@/lib/payments/payment-rules'
import type {PaymentProvider} from '@/types/payment'
const p:PaymentProvider={id:'p',name:'زرین',code:'zarinpal',environment:'sandbox',active:true,priority:1,callbackUrl:'/checkout/callback',supportsRefund:true,supportsPartialRefund:true,supportsVerify:true,currency:'IRT',minAmount:100,maxAmount:100000,timeoutSeconds:30,healthStatus:'healthy',createdAt:'',updatedAt:''}
describe('payment rules',()=>{it('chooses priority provider',()=>expect(chooseProvider([p,{...p,id:'p2',priority:2}],1000).id).toBe('p'));it('rejects amount mismatch',()=>expect(()=>assertPaymentAmount({amount:9,currency:'IRT'},10,'IRT')).toThrow('PAYMENT_AMOUNT_MISMATCH'));it('limits refunds',()=>expect(()=>assertRefundAmount({amount:60},50,100)).toThrow('REFUND_AMOUNT_INVALID'));it('enforces transitions',()=>{expect(canTransitionPayment('pending','succeeded')).toBe(true);expect(canTransitionPayment('failed','succeeded')).toBe(false)});it('validates production secret ref',()=>expect(validateProvider({...p,environment:'production'})).toContain('کلید production باید از secret manager معرفی شود'))})
