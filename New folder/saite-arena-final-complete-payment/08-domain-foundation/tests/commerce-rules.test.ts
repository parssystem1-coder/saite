import {describe,expect,it} from 'vitest'
import {addMoney,assertIdempotencyKey,assertOrderTotals,canTransitionReservation,customerShippingMessage} from '@/lib/domain/commerce-rules'
describe('commerce rules',()=>{it('adds same currency',()=>expect(addMoney({amount:2,currency:'IRT'},{amount:3,currency:'IRT'}).amount).toBe(5));it('rejects currency mismatch',()=>expect(()=>addMoney({amount:2,currency:'IRT'},{amount:3,currency:'IRR'})).toThrow('CURRENCY_MISMATCH'));it('enforces reservation lifecycle',()=>{expect(canTransitionReservation('held','confirmed')).toBe(true);expect(canTransitionReservation('released','confirmed')).toBe(false)});it('makes COD explicit',()=>expect(customerShippingMessage('cash_on_delivery','پست')).toContain('هنگام تحویل'));it('checks idempotency',()=>{expect(()=>assertIdempotencyKey('short')).toThrow();expect(()=>assertIdempotencyKey('checkout:2026:customer:abc123')).not.toThrow()});it('checks order totals',()=>expect(()=>assertOrderTotals({lines:[{lineTotal:{amount:100,currency:'IRT'}}] as never,subtotal:{amount:100,currency:'IRT'},discountTotal:{amount:0,currency:'IRT'},shipping:{customerPayable:{amount:20,currency:'IRT'}} as never,grandTotal:{amount:120,currency:'IRT'}})).not.toThrow())})
