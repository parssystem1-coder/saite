/**
 * جایگزین بستهٔ `server-only` در محیط تست.
 *
 * بستهٔ اصلی وقتی خارج از شرط صادراتی `react-server` ایمپورت شود
 * عمداً خطا می‌دهد — این همان مکانیزمی است که جلوی نشت رمز به
 * باندل کلاینت را می‌گیرد.
 *
 * Vitest آن شرط را اعمال نمی‌کند، پس بدون این stub هر تست لایهٔ
 * سرور با «This module cannot be imported from a Client Component»
 * می‌شکست.
 *
 * ⚠️ این فایل محافظت را ضعیف نمی‌کند: گاردِ واقعی هنگام
 * `next build` اعمال می‌شود، جایی که alias بالا وجود ندارد.
 */
export {}
